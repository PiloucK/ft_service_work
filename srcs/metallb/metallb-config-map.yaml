apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config-map
  annotations:
    metallb.universe.tf/allow-shared-ip: shared
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      - 0.0.0.0
